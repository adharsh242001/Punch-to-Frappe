# Optional: copy this file to .env beside docker-compose.yml only if you want
# to override Docker Compose variables such as the public port.

# Public port for Nginx. The Python app stays internal on container port 8080.
DASHBOARD_PORT=8090
